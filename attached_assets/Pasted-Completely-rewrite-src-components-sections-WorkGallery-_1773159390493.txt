Completely rewrite src/components/sections/WorkGallery.tsx with a new design. Keep the same projects data array and same URLs, but rebuild everything else.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SECTION HEADER

Replace all English text with Arabic and apply Readex Pro font:

- Remove "MORE WORK" divider line completely
- Section label: 'أعمالي' (text-accent, tracking-widest, text-xs)
- Main heading: 'مشاريع سلّمتها وهي شغّالة'
  - font: var(--font-arabic), bold, text-3xl md:text-4xl, color white
  - direction: rtl
- Subtext on the right: '{projects.length}+ مشروع في قطاعات متعددة'
  - color: #4B5563, font-arabic, text-sm

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYOUT — Featured + Grid

Split into TWO sections:

PART A — Featured row (first 2 projects: متجر عودية + Blessing Floral):
- Display as 2 large cards side by side (md:grid-cols-2)
- Each featured card is taller: min-height 280px
- Larger title: text-xl font-bold
- Show full description (no line-clamp)
- Badge top-right
- External link icon top-left (visible on hover)
- Bottom: "زيارة المشروع ←" link in blue, visible always
- Hover: border glows blue, card lifts -6px, subtle blue overlay

PART B — Grid (remaining projects):
- 3 columns on desktop (lg:grid-cols-3), 2 on tablet, 1 on mobile
- Each card: compact, min-height 160px
- Title: text-sm font-semibold
- Description: text-xs, line-clamp-2
- Badge top-right
- "زيارة المشروع ←" appears only on hover (opacity 0 → 1)
- Hover: border rgba(59,130,246,0.3), lift -4px

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CARD DESIGN (apply to ALL cards)

Style:
- background: rgba(255,255,255,0.02)
- border: 1px solid rgba(59,130,246,0.08)
- border-radius: 16px
- padding: featured → p-8, grid → p-5

All text Arabic → add fontFamily: 'var(--font-arabic)', direction: 'rtl'
Title → font-arabic, color white
Description → font-arabic, color #6B7280
Badge → keep badgeColors logic exactly as-is

Hover animation (framer-motion whileHover):
- y: -6 (featured), y: -4 (grid)
- borderColor: rgba(59,130,246,0.28)
- backgroundColor: rgba(255,255,255,0.04)
- transition: duration 0.3, ease easeOut

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VISIT LINK (bottom of each card)

Replace 'Visit Project →' with:

<div
  style={{
    fontSize: '12px',
    color: '#3B82F6',
    fontFamily: 'var(--font-arabic)',
    direction: 'rtl',
    display: 'flex',
    alignItems: 'center',
    gap: '4px',
    marginTop: 'auto',
    paddingTop: '12px',
    borderTop: '1px solid rgba(59,130,246,0.08)',
  }}
>
  زيارة المشروع ←
</div>

Featured cards: always visible
Grid cards: opacity 0, group-hover:opacity-100

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ANIMATIONS

- Section header: staggerContainer + fadeInUp (keep existing)
- Each card: fadeInUp with staggerChildren: 0.06
- Featured cards stagger separately from grid cards

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
KEEP EXACTLY:
- All project data (titles, descriptions, URLs, badges)
- badgeColors object
- All imports (motion, fadeInUp, staggerContainer, ExternalLink)
- Section id="work-gallery"

DO NOT add any images or screenshots — text only cards.